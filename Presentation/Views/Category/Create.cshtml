@* @model Application.DTOs.CategoryRequest

@{
    ViewData["Title"] = "Create Category";
    Layout = "_DashboardLayout";
}

<div class="container">
    <h2 class="mb-4">Create New Category</h2>
    <form asp-action="Create" asp-controller="Category" method="post" class="needs-validation" novalidate>
        <div class="row g-3">
            <div class="col-md-6">
                <div class="form-group">
                    <label asp-for="Name" class="form-label">Category Name</label>
                    <input asp-for="Name" class="form-control"
                           maxlength="100"
                           required
                           data-val="true"
                           data-val-length="Maximum 100 characters allowed"
                           data-val-length-max="100" />
                    <span asp-validation-for="Name" class="text-danger"></span>
                    <small class="form-text text-muted">
                        <span id="nameCounter">0/100</span> characters
                    </small>
                </div>
            </div>
            
        </div>
        <button type="submit" class="btn btn-primary mt-4">Save Category</button>
        <a asp-action="Index" asp-controller="Category" class="btn btn-secondary mt-4 ms-2">Cancel</a>
    </form>
</div>

@section Scripts {
    <script>
        // ADD THIS SCRIPT
        document.getElementById('Name').addEventListener('input', function() {
            const counter = document.getElementById('nameCounter');
            counter.textContent = `${this.value.length}/100 characters`;

            if (this.value.length > 100) {
                this.classList.add('is-invalid');
            } else {
                this.classList.remove('is-invalid');
            }
        });
    </script>
} *@



@model Application.DTOs.CategoryRequest
@{
    ViewData["Title"] = "Create Category";
    Layout = "_DashboardLayout";
}

<!-- REPLACE YOUR EXISTING FORM WITH THIS CARD DESIGN -->
<div class="card shadow border-0 mt-4">
    <div class="card-header bg-primary bg-gradient text-white py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2>Create New Category</h2>
            </div>
        </div>
    </div>
    <div class="card-body p-4">
        <form method="post">
            <div class="form-floating py-2 col-12">
                <input id="Name" asp-for="Name" class="form-control border-0 shadow"
                       maxlength="100"
                       required />
                <label asp-for="Name" class="ms-2 text-muted"></label>
                <span asp-validation-for="Name" class="text-danger"></span>
                <div class="text-end mt-1">
                    <small class="text-muted">
                        <span id="charCount">0</span>/100 characters
                    </small>
                </div>
            </div>

            <div class="row pt-2">
                <div class="col-6 col-md-3">
                    <button type="submit" class="btn btn-primary form-control">
                        <i class="bi bi-check-circle"></i> Create
                    </button>
                </div>
                <div class="col-6 col-md-3">
                    <a asp-action="Index" class="btn btn-outline-secondary form-control">
                        <i class="bi bi-x-circle"></i> Cancel
                    </a>
                </div>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <script>
        // ADD THIS SCRIPT FOR CHARACTER COUNTING
        document.getElementById('Name').addEventListener('input', function() {
            document.getElementById('charCount').textContent = this.value.length;
        });
    </script>
}

